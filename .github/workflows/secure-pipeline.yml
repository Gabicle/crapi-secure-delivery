name: Secure Delivery Pipeline

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository and submodule
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Install Gitleaks (pinned)
        env:
          GITLEAKS_VERSION: "8.30.1"
        run: |
          curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -o gitleaks.tar.gz
          tar -xzf gitleaks.tar.gz gitleaks
          sudo mv gitleaks /usr/local/bin/

      - name: Run Gitleaks (current-state scan)
        run: |
          gitleaks dir -s app --report-format json --report-path gitleaks-report.json

      - name: Upload Gitleaks report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks-report
          path: gitleaks-report.json

  sast:
    runs-on: ubuntu-latest
    needs: secrets
    steps:
      - run: echo "sast stage placeholder"

  sca:
    runs-on: ubuntu-latest
    needs: sast
    steps:
      - run: echo "sca stage placeholder"

  iac:
    runs-on: ubuntu-latest
    needs: sca
    steps:
      - run: echo "iac stage placeholder"

  build:
    runs-on: ubuntu-latest
    needs: iac
    steps:
      - run: echo "build stage placeholder"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "deploy stage placeholder"

  dast:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - run: echo "dast stage placeholder"
